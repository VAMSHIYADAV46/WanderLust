<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Oops! Something Went Wrong</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            text-align: center;
        }
        .error-container {
            max-width: 500px;
            padding: 40px;
            border-radius: 10px;
            background: white;
            box-shadow: 0px 10px 30px rgba(0, 0, 0, 0.1);
        }
        .error-code {
            font-size: 6rem;
            font-weight: bold;
            color: #dc3545;
        }
        .error-message {
            font-size: 1.5rem;
            font-weight: 600;
            color: #343a40;
        }
        .error-description {
            font-size: 1rem;
            color: #6c757d;
        }
    </style>
</head>
<body>

    <div class="error-container">
        <div class="error-code"><%= err.status %></div>
        <div class="error-message"><%= err.message %></div>
        <p class="error-description">The page you are looking for might have been removed, had its name changed, or is temporarily unavailable or you made any mistake.</p>
        <a href="/listings" class="btn btn-danger mt-3">Go Back Home</a>
    </div>

</body>
</html>
